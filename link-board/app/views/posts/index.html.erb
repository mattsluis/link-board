<% for post in @posts %>
  <div class="well well-sm">
    <h4>
      <a href="<%= post.link %>">
        <%= post.title %>
      </a>
    </h4>
    <p>
      by <%= post.user.name %>
    </p>
  </div>
<% end %>